// Minimal stub for WebContainer integration. Real WebContainer API runs in-browser.
export async function startWebContainer() {
  // Placeholder: return fake status
  return { ok: true, message: 'webcontainer stub started' }
}
